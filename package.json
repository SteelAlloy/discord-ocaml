{
  "description": "Runs OCaml code via chat on Discord",
  "version": "0.1.1",
  "scripts": {
    "start": "pm2 start 0",
    "start-node": "node index.js --unhandled-rejections=strict"
  },
  "dependencies": {
    "discord.js": "^12.1.1",
    "standard": "^14.3.3"
  }
}
